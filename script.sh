#!/bin/bash

# When not available checkout the EOS source

# Apply all the patches in the patch.d directory

# Perform the build

# Assuming all went ok

# Spit out a env file so we have a "sandboxed" version
